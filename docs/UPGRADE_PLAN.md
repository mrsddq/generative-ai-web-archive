# Upgrade Plan

This repo can stay archived, or it can become a small portfolio app if it receives the missing operational layer.

## Before Making It Portfolio-Facing

- [ ] Add Flask tests for the health route and translation route.
- [ ] Add screenshots of the UI flow.
- [ ] Document required Azure Translator environment variables.
- [ ] Add deployment notes for Render, Azure App Service, or Docker.
- [ ] Confirm `.env` is ignored and `.env.example` is complete.

## Suggested Framing

Present it as a small Flask integration project, not as a full generative AI platform.
